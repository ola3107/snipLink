import { LinkDetails } from "./definations"
import { EditLinkDetails } from "./definations"




// export async function getDataById(id: string): Promise<EditLinkDetails> {
//     const links = await getLinks();
//     const link = links.find(link => link.id === id);
  
//     if (!link) {
//       throw new Error(`Link with id ${id} not found`);
//     }
  
//     return {
//       name: link.name,
//       link: link.link,
//       customize: link.customize,
//     };
//   }